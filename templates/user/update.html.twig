{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Mon profil{% endblock %}

{% block main %}
    <h1>Mon profil</h1>
    <div>
        <div class="row d-flex justify-content-center">
            <div class="col-md-6">
                {{ form_start(UserForm) }}
                {{ form_widget(UserForm) }}
                <div class="form-group button_center">
                    <button class="btn btn-primary" type="submit" class="btn">Enregistrer</button>
                    <button class="btn btn-primary" type="button"><a href="{{ path('home') }}"></a>Annuler</button>
                </div>
                {{ form_end(UserForm) }}
            </div>
        </div>
    </div>
{% endblock %}
