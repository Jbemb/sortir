{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block main %}

    <h1>Créer un nouvel utilisateur</h1>
<div class="row d-flex justify-content-center">
    <div class="col-md-6">

    {{ form_start(newUserForm) }}
    {{ form_row(newUserForm.username) }}

    {{ form_row(newUserForm.password) }}
    {{ form_row(newUserForm.lastName) }}
    {{ form_row(newUserForm.firstName) }}
    {{ form_row(newUserForm.email) }}
    {{ form_row(newUserForm.telephone) }}
    {{ form_row(newUserForm.isActive) }}
    {{ form_row(newUserForm.campus) }}

        <div class="button_center">
            {{ form_row(newUserForm.add) }}
            {{ form_end(newUserForm) }}
            <div class="form-group">
                <a href="{{ path('home') }}" class="btn btn-primary">Annuler</a>
            </div>
        </div>





    </div>
</div>
{% endblock %}
